{
  "name": "burger-house",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "setup-hooks": "git config core.hooksPath .githooks"
  },
  "engines": {
    "node": ">=20"
  }
}
